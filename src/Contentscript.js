import React from 'react';
import ReactDOM from 'react-dom';

const MyButton = () => {
  return (
    <button>My Button</button>
  );
};

// Find the specific element on the YouTube page where you want to insert your button


// Render your React button component inside the target element
ReactDOM.render(<MyButton />, targetElement);